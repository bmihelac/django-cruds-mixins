============
Installation
============

At the command line::

    $ easy_install django-cruds-mixins

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-cruds-mixins
    $ pip install django-cruds-mixins
